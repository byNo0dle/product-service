server:
  port: '9004'
spring:
  data:
    mongodb:
      database: bank
      port: '27017'
      host: localhost
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
